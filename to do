18.6:

to fix:
    add authorization in middleware.
    use link in sign me up (not form)
    render all pages in the template
    use connection pool (create multiple connections, handle db errors)
    security: validate user id, encrypt cookie.

homework:
    read about how browser works (after getting a string from server)


26.6:

send ajax on sign up, check if user name exists and only then sign up.
